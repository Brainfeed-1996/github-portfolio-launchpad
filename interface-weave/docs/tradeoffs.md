# Interface Weave Trade-offs

## Abstraction depth
A deeper abstraction layer improves consistency, but can become hard to learn if it hides too much behavior.

## Explicit state graphs
They improve correctness and readability, but introduce more upfront modeling cost.

## Accessibility by default
This is the right default, though it may require more careful component API design from the start.
